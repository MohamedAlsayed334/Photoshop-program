#include <iostream>
#include <string>
#include "Image_Class.h"
using namespace std;
void WanoSunlight(Image &img)
{
    for (int i = 0; i < img.width; ++i)
    {
        for (int j = 0; j < img.height; ++j)
        {
            // nothing untill now
        }
    }
}